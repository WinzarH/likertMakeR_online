# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
#
# See https://github.com/r-lib/actions/tree/master/examples#readme for
# additional example workflows available for the R community.

name: R (Shiny app)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        r-version: ["release"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.r-version }}

      # If you use a renv.lock, this will restore deps and cache them
      - name: Restore dependencies (renv)
        uses: r-lib/actions/setup-renv@v2
        with:
          cache-version: 2

      # Smoke test: ensure the app file(s) parse and key packages load
      - name: Smoke test
        run: |
          Rscript -e "sessionInfo()"
          Rscript -e "library(shiny)"
          Rscript -e "source('app.R'); cat('app.R sourced OK\n')"
